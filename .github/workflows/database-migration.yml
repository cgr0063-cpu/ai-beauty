name: Production Database Migration

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  migrate:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ai-beauty/backend

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Build backend
        run: npm run build

      - name: Run production migration
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          DB_DRIVER: postgres
          NODE_ENV: production
        run: npm run migrate:prod
